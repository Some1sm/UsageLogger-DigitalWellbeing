<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="UsageLogger.Views.VisualReportDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:l="using:WinUI3Localizer"
    mc:Ignorable="d"
    l:Uids.Uid="ReportDialog"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1100</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">850</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">600</x:Double>
    </ContentDialog.Resources>

    <Grid RowSpacing="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status / Subtitle -->
        <TextBlock x:Name="StatusText" Grid.Row="0" 
                   Text="Share or save your weekly infographic."
                   Style="{StaticResource CaptionTextBlockStyle}" 
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                   Margin="0,0,0,4"/>

        <!-- Image Preview Card -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" Padding="8">
            <Grid MinHeight="250">
                <ProgressRing x:Name="LoadingRing" IsActive="True" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Image x:Name="ReportImagePreview" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Feedback Notification -->
        <InfoBar x:Name="ReportInfoBar" Grid.Row="2" IsOpen="False" Severity="Success" Margin="0,4,0,0"/>
    </Grid>
</ContentDialog>


