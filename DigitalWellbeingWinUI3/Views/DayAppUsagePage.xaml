<Page
    x:Class="DigitalWellbeingWinUI3.Views.DayAppUsagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Date Header (Verified Good) -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
                <Border Background="#20FFFFFF" CornerRadius="8" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                         <TextBlock Text="📅" VerticalAlignment="Center" Margin="0,0,10,0"/>
                         <TextBlock Text="{x:Bind ViewModel.StrLoadedDate, Mode=OneWay}" Style="{ThemeResource TitleTextBlockStyle}" FontSize="18" VerticalAlignment="Center"/>
                         <Button Content="&lt;" Command="{x:Null}" IsEnabled="{x:Bind ViewModel.CanGoPrev, Mode=OneWay}" Click="BtnPrev_Click" Background="Transparent" BorderThickness="0" Margin="10,0,0,0"/>
                         <Button Content="&gt;" Command="{x:Null}" IsEnabled="{x:Bind ViewModel.CanGoNext, Mode=OneWay}" Click="BtnNext_Click" Background="Transparent" BorderThickness="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- ROW 1: CHARTS (Injected via Code-Behind) -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <!-- Bar Chart Container -->
                <Border Grid.Column="0" Background="#1AFFFFFF" CornerRadius="12" BorderBrush="#30FFFFFF" BorderThickness="1" Padding="15" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="300"/> <!-- Fixed Height for Chart Content -->
                        </Grid.RowDefinitions>
                        <TextBlock Text="Daily Activity" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFFFF" Margin="0,0,0,10"/>
                        <ContentControl x:Name="BarChartContainer" Grid.Row="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                    </Grid>
                </Border>

                <!-- Pie Chart Container -->
                <Border Grid.Column="1" Background="#1AFFFFFF" CornerRadius="12" BorderBrush="#30FFFFFF" BorderThickness="1" Padding="15" Margin="10,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="300"/> <!-- Fixed Height for Chart Content -->
                        </Grid.RowDefinitions>
                        <TextBlock Text="Usage Summary" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFFFF" Margin="0,0,0,10"/>
                        <ContentControl x:Name="PieChartContainer" Grid.Row="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- ROW 2: APP LIST (Verified Working) -->
            <GridView Grid.Row="2" x:Name="AppListGridView" 
                      ItemsSource="{x:Bind ViewModel.DayListItems, Mode=OneWay}" 
                      SelectionMode="None" Margin="0,10,0,0"
                      SizeChanged="AppListGridView_SizeChanged"
                      ScrollViewer.VerticalScrollMode="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,0,6,12"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12" Background="#1AFFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" Padding="15,12" RightTapped="Grid_RightTapped">
                        <Border.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Set Time Limit" Icon="Clock" Click="MenuFlyoutItem_SetTimeLimit_Click" Tag="{Binding ProcessName}"/>
                                <MenuFlyoutSubItem Text="Set Category" Icon="Tag">
                                    <MenuFlyoutItem Text="Productivity" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                    <MenuFlyoutItem Text="Entertainment" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                    <MenuFlyoutItem Text="Utility" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                    <MenuFlyoutItem Text="Social" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                    <MenuFlyoutItem Text="Game" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                    <MenuFlyoutItem Text="Other" Click="MenuFlyoutItem_SetAppTag_Click" Tag="{Binding ProcessName}"/>
                                </MenuFlyoutSubItem>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="Exclude App" Icon="Delete" Click="MenuFlyoutItem_ExcludeApp_Click" Tag="{Binding ProcessName}"/>
                            </MenuFlyout>
                        </Border.ContextFlyout>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- ICON -->
                            <Image Source="{Binding IconSource}" Width="36" Height="36" Margin="0,0,15,0"/>
                            
                            <!-- DETAILS -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                     <TextBlock Text="{Binding ProcessName}" FontWeight="SemiBold" FontSize="15" Margin="0,0,10,0"/>
                                     <Border Background="#3000FFFF" CornerRadius="10" Padding="8,2" VerticalAlignment="Center">
                                         <TextBlock Text="{Binding StrAppTag}" Foreground="{Binding BrushAppTag}" FontSize="11" FontWeight="SemiBold"/>
                                     </Border>
                                </StackPanel>
                                <!-- GRADIENT PROGRESS BAR TEST -->
                                <ProgressBar Value="{Binding Percentage}" Maximum="100" Height="4" CornerRadius="2" HorizontalAlignment="Stretch">
                                    <ProgressBar.Foreground>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#00FFFF" Offset="0.0"/>
                                            <GradientStop Color="#0000FF" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </ProgressBar.Foreground>
                                </ProgressBar>
                            </StackPanel>
                            
                            <!-- DURATION -->
                            <TextBlock Grid.Column="2" Text="{Binding StrDuration}" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="14" Margin="15,0,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        </Grid>
    </ScrollViewer>
</Page>
