<Page
    x:Class="DigitalWellbeingWinUI3.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigitalWellbeingWinUI3.Views"
    xmlns:vm="using:DigitalWellbeingWinUI3.ViewModels"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Controls Header -->
        <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,20">
            <Grid HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="25"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Start Date -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Start Date" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,5"/>
                <CalendarDatePicker Grid.Row="1" Grid.Column="0" Date="{Binding StartDate, Mode=TwoWay}" Width="150" VerticalAlignment="Top"/>

                <!-- End Date -->
                <TextBlock Grid.Row="0" Grid.Column="2" Text="End Date" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,5"/>
                <CalendarDatePicker Grid.Row="1" Grid.Column="2" Date="{Binding EndDate, Mode=TwoWay}" Width="150" VerticalAlignment="Top"/>
                
                <!-- Generate Button -->
                <Button Grid.Row="1" Grid.Column="4" Content="Generate Report" Command="{x:Bind ViewModel.GenerateChartCommand}" Width="140" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Top"/>

                <!-- Toggle -->
                <TextBlock Grid.Row="0" Grid.Column="6" Text="Group by Category" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,5"/>
                <ToggleSwitch Grid.Row="1" Grid.Column="6" IsOn="{Binding IsTagView, Mode=TwoWay}" OnContent="Tags" OffContent="Apps" VerticalAlignment="Top" Margin="0,-5,0,0"/>
            </Grid>
        </Border>

        <!-- Chart Area -->
        <Grid Grid.Row="1">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Distribution Analysis" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    
                    <!-- INJECTED CHART CONTAINER -->
                    <ContentControl x:Name="HistoryChartContainer" Grid.Row="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" MinHeight="400"/>
                    
                    <!-- Loading Indicator -->
                    <ProgressRing Grid.Row="1" IsActive="{Binding IsLoading}" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Page>
