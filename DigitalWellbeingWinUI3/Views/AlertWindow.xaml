<Window
    x:Class="DigitalWellbeingWinUI3.Views.AlertWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,0,20">
            <Run Text="You have exceeded the time limit for " />
            <Run x:Name="RunProgramName" FontWeight="Bold"/>
            <Run Text=" ("/>
            <Run x:Name="RunProcessName" />
            <Run Text=")"/>
            <Run Text=". Your current usage time is "/>
            <Run x:Name="RunUsageTime" FontWeight="Bold"/>
            <Run Text=" and your time limit is up to "/>
            <Run x:Name="RunTimeLimit" FontWeight="Bold"/>
            <Run Text="."/>
        </TextBlock>

        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="20">
            <Button x:Name="BtnCloseApp" Content="Close the App" Click="BtnCloseApp_Click" />
            <Button x:Name="BtnOK" Content="OK, Thanks!" Click="BtnOK_Click" Style="{ThemeResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
