<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DigitalWellbeingWinUI3.Views.Controls.Win2DTimelineControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    d:DesignHeight="1440"
    d:DesignWidth="400">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
            CornerRadius="12" 
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
            BorderThickness="1" 
            Margin="7.5,15,7.5,15" 
            Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Date Header -->
                <RowDefinition Height="*"/>    <!-- Timeline Canvas -->
            </Grid.RowDefinitions>

            <!-- Date Header -->
            <TextBlock x:Name="HeaderText" 
                       Grid.Row="0" 
                       Style="{ThemeResource BodyStrongTextBlockStyle}" 
                       HorizontalAlignment="Center" 
                       Margin="0,12,0,4"/>

            <!-- ScrollViewer containing the Win2D canvas -->
            <ScrollViewer x:Name="MainScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="ContentGrid" MinHeight="1440">
                     <canvas:CanvasVirtualControl x:Name="TimelineCanvas" RegionsInvalidated="TimelineCanvas_RegionsInvalidated" PointerMoved="TimelineCanvas_PointerMoved" PointerExited="TimelineCanvas_PointerExited" />
                     
                     <!-- Tooltip Overlay -->
                     <Border x:Name="TooltipBorder" 
                             Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" 
                             BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                             BorderThickness="1" 
                             CornerRadius="4" 
                             Padding="8,4,8,4" 
                             VerticalAlignment="Top" 
                             HorizontalAlignment="Left"
                             Visibility="Collapsed" 
                             IsHitTestVisible="False">
                        <TextBlock x:Name="TooltipText" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" FontSize="12"/>
                     </Border>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
