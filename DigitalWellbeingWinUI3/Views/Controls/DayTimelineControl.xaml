<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DigitalWellbeingWinUI3.Views.Controls.DayTimelineControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:DigitalWellbeingWinUI3.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="1440"
    d:DesignWidth="400">

    <UserControl.Resources>
        <viewmodels:BoolToVisibilityConverter x:Key="BoolToVis" xmlns:viewmodels="using:DigitalWellbeingWinUI3.Converters" />
    </UserControl.Resources>

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Margin="7.5,15,7.5,15" Padding="15">
        <Grid x:Name="ContainerGrid" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Date Header -->
                <RowDefinition Height="*"/>    <!-- Timeline -->
            </Grid.RowDefinitions>

            <!-- Optional Date Header for Column -->
            <TextBlock x:Name="HeaderDate" Grid.Row="0" Text="{Binding DateString, Mode=OneWay}" 
                       Style="{ThemeResource BodyStrongTextBlockStyle}" HorizontalAlignment="Center" Margin="0,12,0,4"
                       Visibility="Visible"/> 

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/> <!-- Labels -->
                        <ColumnDefinition Width="*"/>  <!-- Canvas -->
                    </Grid.ColumnDefinitions>

                    <!-- Grid Lines & Labels -->
                    <ItemsControl Grid.ColumnSpan="2" ItemsSource="{Binding GridLines, Mode=OneWay}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="{Binding Width}">
                                    <Grid.RenderTransform>
                                        <TranslateTransform Y="{Binding Top}"/>
                                    </Grid.RenderTransform>
                                    
                                    <!-- Line: Spans whole width -->
                                    <Border BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="0,1,0,0" Opacity="{Binding Opacity}" Margin="60,0,0,0" Height="1" VerticalAlignment="Top"/>
                                    
                                    <!-- Label: In first 60px -->
                                    <TextBlock Text="{Binding TimeText}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,-10,50,0" Opacity="0.5" FontSize="{Binding FontSize}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Sessions -->
                    <Canvas Grid.Column="0" Grid.ColumnSpan="2" Height="{Binding CanvasHeight, Mode=OneWay}" VerticalAlignment="Top">
                        <ItemsControl ItemsSource="{Binding SessionBlocks, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Width="{Binding Width}" Height="{Binding Height}" ToolTipService.ToolTip="{Binding Title}">
                                        <Grid.RenderTransform>
                                            <TranslateTransform X="{Binding Left}" Y="{Binding Top}" />
                                        </Grid.RenderTransform>
                                        
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/> <!-- Audio Column -->
                                        </Grid.ColumnDefinitions>

                                        <!-- MAIN APP -->
                                        <Grid Grid.Column="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="4"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Rectangle Grid.Column="0" Fill="{Binding BackgroundColor}" RadiusX="2" RadiusY="2"/>
                                            <Border Grid.Column="1" Background="{Binding BackgroundColor}" Opacity="0.2" CornerRadius="0,4,4,0"/>
                                            <StackPanel Grid.Column="1" Padding="8,2" VerticalAlignment="Center" Visibility="{Binding ShowDetails, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <StackPanel Orientation="Horizontal" Spacing="4">
                                                    <TextBlock Text="{Binding DurationText}" FontSize="10" Opacity="0.7"/>
                                                    <Border Background="{ThemeResource SystemFillColorCautionBrush}" CornerRadius="2" Padding="2,0" Visibility="{Binding IsAfk}">
                                                        <TextBlock Text="AFK" FontSize="8" Foreground="Black"/>
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>

                                        <!-- AUDIO SOURCES -->
                                        <Grid Grid.Column="1" Width="100" Margin="4,0,0,0" Visibility="{Binding HasAudio, Converter={StaticResource BoolToVis}}">
                                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Opacity="0.8">
                                                <StackPanel VerticalAlignment="Center" Padding="4">
                                                    <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,0,0,2">
                                                        <FontIcon Glyph="&#xE767;" FontSize="10" Opacity="0.6"/> <!-- Volume Icon -->
                                                        <TextBlock Text="Audio" FontSize="9" Opacity="0.6" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding AudioSourcesText}" FontSize="10" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" MaxLines="2"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Indicator -->
                        <Grid Width="{Binding ContentWidth, Mode=OneWay}" Height="2" Canvas.Left="0" Canvas.Top="{Binding CurrentTimeTop, Mode=OneWay}" Visibility="{Binding CurrentTimeVisibility, Mode=OneWay}">
                            <Rectangle Fill="#FF5252" />
                            <Ellipse Width="8" Height="8" Fill="#FF5252" HorizontalAlignment="Left" Margin="-4,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </Canvas>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
