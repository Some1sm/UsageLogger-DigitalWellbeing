<Page
    x:Class="DigitalWellbeingWinUI3.Views.MiscDataPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigitalWellbeingWinUI3.Views"
    xmlns:vm="using:DigitalWellbeingWinUI3.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:l="using:WinUI3Localizer"
    mc:Ignorable="d"
    Background="Transparent"
    SizeChanged="RootPage_SizeChanged">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid x:Name="HeaderGrid" Grid.Row="0" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="HeaderTitlePanel" Grid.Column="0">
                <TextBlock l:Uids.Uid="MiscData_Title" Text="Fun Facts" Style="{ThemeResource TitleTextBlockStyle}"/>
                <TextBlock l:Uids.Uid="MiscData_Subtitle" Text="Daily Snapshot &amp; Insights" 
                       Style="{ThemeResource SubtitleTextBlockStyle}" Opacity="0.7"/>
            </StackPanel>
            
            <!-- Date Selector (Dashboard Style) -->
            <StackPanel x:Name="HeaderDatePanel" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <Button Background="Transparent" VerticalAlignment="Center" BorderThickness="0" Padding="0" Margin="0,0,10,0" ToolTipService.ToolTip="Select Date">
                            <TextBlock Text="📅" VerticalAlignment="Center"/>
                            <Button.Flyout>
                                <Flyout x:Name="CalendarFlyout" Placement="Bottom">
                                    <CalendarView x:Name="CalendarPicker" SelectionMode="Single" SelectedDatesChanged="CalendarPicker_SelectedDatesChanged" />
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                        <TextBlock Text="{x:Bind ViewModel.StrLoadedDate, Mode=OneWay}" Style="{ThemeResource SubtitleTextBlockStyle}" FontSize="16" VerticalAlignment="Center" Width="220" TextAlignment="Center"/>
                        <Button Content="&lt;" Click="BtnPrev_Click" Background="Transparent" BorderThickness="0" Margin="5,0,0,0"/>
                        <Button Content="&gt;" IsEnabled="{x:Bind ViewModel.CanGoNext, Mode=OneWay}" Click="BtnNext_Click" Background="Transparent" BorderThickness="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Stats Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Stats, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VariableSizedWrapGrid Orientation="Horizontal" ItemWidth="{Binding ResponsiveItemWidth, ElementName=RootPage, Mode=OneWay}" ItemHeight="160" MaximumRowsOrColumns="6"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:StatItem">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="16" Margin="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Icon -->
                                <FontIcon Grid.Row="0" Glyph="{x:Bind Icon}" FontSize="18" 
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" 
                                          HorizontalAlignment="Left" Margin="0,0,0,4"/>

                                <!-- Value -->
                                <TextBlock Grid.Row="1" Text="{x:Bind Value}" 
                                           Style="{ThemeResource SubtitleTextBlockStyle}" 
                                           FontWeight="SemiBold"/>

                                <!-- Label + Description -->
                                <StackPanel Grid.Row="2" Margin="0,4,0,0">
                                    <TextBlock Text="{x:Bind Label}" 
                                               Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind Description}" 
                                               Style="{ThemeResource CaptionTextBlockStyle}" 
                                               Opacity="0.7" TextWrapping="Wrap" MaxLines="2"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
