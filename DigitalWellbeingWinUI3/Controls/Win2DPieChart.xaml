<UserControl
    x:Class="DigitalWellbeingWinUI3.Controls.Win2DPieChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigitalWellbeingWinUI3.Controls"
    xmlns:models="using:DigitalWellbeingWinUI3.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Use App resources or local definition if needed -->
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" MinWidth="120" MaxWidth="180"/>
        </Grid.ColumnDefinitions>

        <canvas:CanvasControl x:Name="Canvas" Grid.Column="0" Draw="Canvas_Draw" PointerMoved="Canvas_PointerMoved" PointerExited="Canvas_PointerExited" ClearColor="Transparent"/>

        <ListView x:Name="LegendList" Grid.Column="1" ItemsSource="{x:Bind LegendItems, Mode=OneWay}" 
                  SelectionMode="None" Margin="10,0,0,0" Padding="0" VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PieChartItem">
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,2" Background="Transparent" 
                                PointerEntered="LegendItem_PointerEntered" PointerExited="LegendItem_PointerExited">
                        <Ellipse Width="10" Height="10" Fill="{x:Bind Color, Converter={StaticResource ColorToBrushConverter}}"/>
                        <TextBlock FontSize="12" VerticalAlignment="Center">
                            <Run Text="{x:Bind Name}"/>
                            <Run Text=" "/>
                            <Run Text="{x:Bind Percentage, Converter={StaticResource PercentConverter}}" Foreground="Gray" FontSize="11"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="MinHeight" Value="0"/>
                    <Setter Property="Padding" Value="4,2"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
